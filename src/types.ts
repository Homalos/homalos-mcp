// 类型定义（CommonJS 中作为注释保留）
// KlineData: { time, open, high, low, close, volume, openInterest }
// TradingDay: { date, klines[] }
// FuturesResponse: { symbol, tradingDays[] }
// RawKlineData: (string | number)[]
// KDValue: { date, time, k, d }
// KDResponse: { symbol, parameters, kdValues[] }
// MACDValue: { date, time, macd, signal, histogram }
// MACDResponse: { symbol, parameters, macdValues[] }

module.exports = {};
